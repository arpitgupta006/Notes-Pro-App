pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven {
            url "https://storage.googleapis.com/r8-releases/raw/master"
        }
        mavenCentral()

        jcenter()
    }
}
rootProject.name = "Notes Pro App"
include ':app'
